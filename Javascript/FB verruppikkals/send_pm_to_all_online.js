
var inputs = document.getElementsByClassName('l1'); 
// class name of the chat sidebar

for(var i=0;i<inputs.length;i++) { 

    // click on the focused element
    inputs[i].click(); 
    
var x = prompt('message');

var q = document.getElementByClassName('uiTextareaAutogrow _552m');

var cbtner = document.getElementByClassName('_1ri uiButton uiButtonConfirm');
cbtner.click();
    // get focus to the textfield by ClassName 'uiTextareaAutogrow _552m'

    // and click on the current 'close button'
}
